RSpec.describe Rspec::Golden::Files do
  it "has a version number" do
    expect(Rspec::Golden::Files::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
